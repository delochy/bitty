{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "AI Side Quest Mascot의 기본 권한 — 창/트레이 조작은 Rust 쪽에서 직접 하고, 웹뷰(JS)가 별도 Tauri API를 호출하지 않으므로 최소한만 둔다.",
  "windows": ["mascot"],
  "permissions": ["core:default"]
}
